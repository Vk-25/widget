<?xml version="1.0" encoding="utf-8"?>
<!--
    widget_flip_clock.xml
    ─────────────────────
    • Digit row: H1 H2 · M1 M2 · S1 S2  (all ImageViews, bitmaps set in code)
    • Date strip: ImageView iv_date — full-width bitmap drawn in FlipClockWidget
      giving pixel-perfect control over font size, weight, colour and shadow.
    • Transparent root — wallpaper shows through between tiles.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingLeft="4dp"
        android:paddingRight="4dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <!-- ── Digit row ─────────────────────────────────────────────── -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <ImageView android:id="@+id/iv_h1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"
                android:layout_marginEnd="2dp"/>
            <ImageView android:id="@+id/iv_h2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"
                android:layout_marginEnd="2dp"/>

            <ImageView android:id="@+id/iv_sep1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"
                android:layout_marginEnd="2dp"/>

            <ImageView android:id="@+id/iv_m1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"
                android:layout_marginEnd="2dp"/>
            <ImageView android:id="@+id/iv_m2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"
                android:layout_marginEnd="2dp"/>

            <ImageView android:id="@+id/iv_sep2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"
                android:layout_marginEnd="2dp"/>

            <ImageView android:id="@+id/iv_s1"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"
                android:layout_marginEnd="2dp"/>
            <ImageView android:id="@+id/iv_s2"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitXY"/>

        </LinearLayout>

        <!-- ── Date strip — full-width bitmap ────────────────────────── -->
        <!--
            Rendered by FlipClockWidget.buildDateBitmap():
              [DayName] · [DD] [MonthName] · [YYYY]
            Bold + drop shadow for visibility on any wallpaper.
            adjustViewBounds keeps the bitmap at its natural aspect ratio.
        -->
        <ImageView
            android:id="@+id/iv_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scaleType="fitStart"
            android:adjustViewBounds="true"
            android:layout_gravity="start"/>

    </LinearLayout>

</FrameLayout>